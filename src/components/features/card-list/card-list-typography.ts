/**
 * Card list 카드 내부 텍스트는 공용 타이포 토큰(titleS/caption)의
 * 1.6배 line-height를 그대로 쓰면 좁은 카드 안에서 과도한 여백으로 보여
 * 카드 전용으로만 더 촘촘한 line-height를 사용한다.
 */
export const CARD_TITLE_LINE_HEIGHT = 22;
export const CARD_META_LINE_HEIGHT = 16;
